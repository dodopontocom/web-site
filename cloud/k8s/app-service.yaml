apiVersion: v1
kind: Service
metadata:
  labels:
    label-key: deployment-dev
  name: web-site-service
spec:
  type: ClusterIP
  ports:
  - name: service-port
    port: 80
    protocol: TCP
  selector:
    label-key: deployment-dev